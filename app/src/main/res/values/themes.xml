<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.LuxeVista" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.LuxeVista" parent="Base.Theme.LuxeVista" />
    
    <!-- Optional overlay to control font family for bottom nav labels -->
    <style name="ThemeOverlay.LuxeVista.BottomNav">
        <item name="android:fontFamily">@font/poppins_regular</item>
    </style>
    
    <!-- Splash Screen Theme -->
    <style name="Theme.LuxeVista.Splash" parent="Theme.LuxeVista">
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowBackground">@android:color/white</item>
        <item name="android:windowSplashScreenBackground">@android:color/white</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>
    
    <!-- Filter Button Style -->
    <style name="FilterButtonStyle" parent="Widget.MaterialComponents.Button.OutlinedButton">
        <item name="android:fontFamily">@font/poppins_medium</item>
        <item name="android:textSize">12sp</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:paddingStart">20dp</item>
        <item name="android:paddingEnd">20dp</item>
        <item name="android:minWidth">0dp</item>
        <item name="android:minHeight">0dp</item>
        <item name="cornerRadius">18dp</item>
        <item name="strokeWidth">1dp</item>
        <item name="strokeColor">@color/filter_unselected</item>
        <item name="android:textColor">@color/filter_text_unselected</item>
        <item name="backgroundTint">@android:color/transparent</item>
        <item name="rippleColor">@color/light_blue_background</item>
    </style>
    
    <!-- Promotions Filter Chip Style -->
    <style name="PromotionsFilterChipStyle" parent="Widget.Material3.Chip.Assist.Elevated">
        <item name="android:textColor">@color/dark_blue_primary</item>
        <item name="chipBackgroundColor">@color/light_blue_background</item>
        <item name="chipStrokeColor">@color/dark_blue_primary</item>
        <item name="chipStrokeWidth">1dp</item>
        <item name="android:textSize">12sp</item>
        <item name="android:fontFamily">@font/poppins_medium</item>
    </style>

    
</resources>